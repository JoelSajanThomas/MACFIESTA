import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.joel.macfiesta',
  appName: 'MACFIESTA',
  webDir: 'out',
  server: {
    cleartext: true
  }
};

export default config;